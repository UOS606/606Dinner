spring.application.name=mrdinner

# ================================
# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/mrdinner?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=soojin9433@

# -------------------------------
# ? JPA & Hibernate
# ??? ???? ??? ?? ??/?? (??? ???)
spring.jpa.hibernate.ddl-auto=update

# SQL ?? ?? (???? ???)
spring.jpa.show-sql=true

# -------------------------------
# data.sql ?? ?? ?? (Spring Boot 3.x ??)
# data.sql ??? ???
spring.sql.init.mode=always
# ?? ?? (src/main/resources/data.sql)
spring.sql.init.data-locations=classpath:data.sql
# Hibernate? ???? ?? ? data.sql? ????? ??
spring.jpa.defer-datasource-initialization=true

# ================================
# JWT Configuration
# ?? ??? ???? (?? ? .env ? ????? ??? ??)
jwt.secret=ChangeThisToAReallyLongRandomBase64String_AtLeast_256bit
# ?? ???? (1?? = 3600000 ms)
jwt.expiration-ms=3600000