import React from "react";
import common from "./OrderCommon.module.css";

const CurrentOrder = () => {
  return (
    <main className={common.container}>
      <h2 className={common.title}>현재 주문 내역 페이지 (준비중)</h2>
    </main>
  );
};

export default CurrentOrder;
